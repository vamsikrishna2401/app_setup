name: "test workflow"

on:
  push:
    branches:
      - main

env:
  username: Krishna
  app_name: sample

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: user created
        run: |
          echo "app created by user $username "
        
      - name: app name
        run: echo " name of the application is $app_name " 